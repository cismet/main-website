version: "2"

services:
  nginx:
    image: zeroplusx/middleman
    container_name: dev-www.cismet.de
    restart: always
    network_mode: bridge
    volumes:
      - ./:/src

    ports:
      - "4567:4567"
      - "8888:80"
